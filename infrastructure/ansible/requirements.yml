# Ansible Galaxy Requirements
# Install with: ansible-galaxy install -r requirements.yml

roles:
  # K3s installation role
  - name: xanmanning.k3s
    version: v3.6.2  # Updated for Ansible 2.19+ compatibility

collections:
  - name: ansible.posix
  - name: community.general
