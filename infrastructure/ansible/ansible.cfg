[defaults]
# Suppress deprecation warnings from third-party roles (xanmanning.k3s)
# These are INJECT_FACTS_AS_VARS warnings that will be fixed in Ansible 2.24
deprecation_warnings = False

# SSH settings for VM rebuilds - accept new host keys
# Combined with 'make clean-known-hosts' for full idempotency
host_key_checking = False
