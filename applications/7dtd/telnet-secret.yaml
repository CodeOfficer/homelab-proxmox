# Telnet secret template - password populated by deploy.sh from .secrets/7dtd-telnet-password
# This file is NOT applied directly - deploy.sh uses it as a template
apiVersion: v1
kind: Secret
metadata:
  name: sdtd-telnet
  namespace: sdtd
type: Opaque
stringData:
  password: "${TELNET_PASSWORD}"
